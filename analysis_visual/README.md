## Setup environment for all features other than VLM

- conda env create -f environment_visual.yml

- Install CLIP: `pip install git+https://github.com/openai/CLIP.git`

- Install TransNetV2: `cd TransNetV2 && python setup.py install`


## Video Feature Pipeline

### Shot Detection


## Setup environment for VLM

- conda env create -f environment_vlm.yml